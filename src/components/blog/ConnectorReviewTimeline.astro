<figure
  class="connector-timeline"
  aria-label="Savecraft connector review timeline. OpenAI received a submission in March and approved it in April. Anthropic received two submissions in March, sent an automated support response in April, confirmed the review in May and June, then closed the queue in July and asked for a new submission--illustrated by a flaming train falling from a crumbling track."
>
  <figcaption>
    <strong>A Tale of Submitting to Two App Directories</strong>
    <span>Savecraft connector review, 2026</span>
  </figcaption>

  <div class="timeline-scroll">
    <div class="timeline-board">
      <section class="timeline-lane openai-lane" aria-label="OpenAI timeline">
        <div class="provider" role="heading" aria-level="3">OpenAI</div>
        <div class="track">
          <span class="rail" aria-hidden="true"></span>
          <div class="event column-1">
            <time datetime="2026-03">March</time>
            <span>Submitted</span>
          </div>
          <div class="event approved column-2">
            <span class="happy-arrival" aria-hidden="true">
              <svg viewBox="0 0 96 68">
                <g class="happy-smoke" transform="translate(66 0) scale(-1 1)">
                  <circle cx="20" cy="19" r="4" />
                  <circle cx="13" cy="14" r="5" />
                  <circle cx="6" cy="18" r="3" />
                </g>
                <g class="happy-train" transform="translate(66 0) scale(-1 1)">
                  <path class="happy-cab" d="M34 27h25v24H34z" />
                  <path class="happy-roof" d="M30 27h34l-5-6H35z" />
                  <path class="happy-window" d="M41 32h11v9H41z" />
                  <rect class="happy-boiler" x="7" y="34" width="31" height="17" rx="7" />
                  <path class="happy-stack" d="M13 22h11l-2 5v9h-7v-9z" />
                  <path class="happy-base" d="M5 49h58v5H5z" />
                  <circle class="happy-wheel" cx="18" cy="56" r="6" />
                  <circle class="happy-wheel" cx="49" cy="56" r="6" />
                  <circle class="happy-hub" cx="18" cy="56" r="2" />
                  <circle class="happy-hub" cx="49" cy="56" r="2" />
                </g>
                <text class="toot-caption" x="3" y="8">TOOT!</text>
                <path class="confetti" d="m76 14 4-5m1 15 6 1m-13 6-4 5" />
              </svg>
            </span>
            <span class="check" aria-hidden="true">✓</span>
            <time datetime="2026-04">April</time>
            <span>Approved</span>
          </div>
        </div>
      </section>

      <section class="timeline-lane anthropic-lane" aria-label="Anthropic timeline">
        <div class="provider" role="heading" aria-level="3">Anthropic</div>
        <div class="track">
          <span class="rail" aria-hidden="true"></span>
          <div class="event column-1">
            <time datetime="2026-03">March</time>
            <span>Submitted twice</span>
          </div>
          <div class="event column-2">
            <time datetime="2026-04">April</time>
            <span>Customer support escalation to nowhere</span>
          </div>
          <div class="event column-3">
            <time datetime="2026-05">May</time>
            <span>"In the standard process"</span>
          </div>
          <div class="event column-4">
            <time datetime="2026-06">June</time>
            <span>"In our review queue"</span>
          </div>
          <div class="event derail column-5">
            <span class="derailment" aria-hidden="true">
              <svg viewBox="0 0 150 90">
                <g class="smoke">
                  <circle class="smoke-puff smoke-puff-1" cx="61" cy="13" r="7" />
                  <circle class="smoke-puff smoke-puff-2" cx="48" cy="8" r="9" />
                  <circle class="smoke-puff smoke-puff-3" cx="34" cy="13" r="6" />
                </g>

                <g class="wrecked-train">
                  <path class="flame flame-back" d="M28 29c-7-7 2-13-1-20 8 4 8 10 8 13 5-5 8-10 7-16 10 9 8 18 3 26z" />
                  <path class="flame flame-top" d="M62 25c-5-8 3-13 1-20 8 6 8 12 7 16 4-3 6-6 7-10 6 8 2 15-4 20z" />
                  <path class="train-roof" d="M18 21h35l-5-7H23z" />
                  <path class="train-cab" d="M22 21h27v30H22z" />
                  <path class="train-window" d="M29 27h13v11H29z" />
                  <rect class="train-boiler" x="47" y="29" width="43" height="22" rx="8" />
                  <path class="train-stack" d="M68 12h15l-3 6v13H70V18z" />
                  <path class="train-dome" d="M52 23h11v8H52z" />
                  <path class="train-nose" d="M89 34h8v18h-8z" />
                  <path class="cowcatcher" d="m96 43 15 15H91z" />
                  <path class="train-base" d="M17 49h82v7H17z" />
                  <circle class="wheel" cx="34" cy="58" r="9" />
                  <circle class="wheel" cx="76" cy="58" r="9" />
                  <circle class="wheel-hub" cx="34" cy="58" r="3" />
                  <circle class="wheel-hub" cx="76" cy="58" r="3" />
                  <path class="drive-rod" d="M34 58h42" />
                  <path class="flame flame-front" d="M89 37c-4-8 3-13 1-19 8 5 7 11 7 14 4-3 6-7 6-11 7 8 3 16-4 22z" />
                </g>

                <g class="escapee">
                  <text class="panic-caption" x="109" y="12">NOPE</text>
                  <circle class="person-head" cx="125" cy="23" r="4" />
                  <path class="person" d="m125 27 2 13m-1-8-9-4m9 4 9-6m-8 14-7 10m7-10 9 7" />
                  <path class="flame person-flame" d="M132 45c-4-5 2-8 0-13 6 4 6 8 5 11 3-2 4-4 4-7 5 6 1 11-5 14z" />
                </g>

                <g class="crumbling-track">
                  <path class="falling-rail" d="m81 70 17 7m-15 2 14 7m7-4 13 3" />
                  <path class="falling-sleeper" d="m91 67 4 14m13-9 7 13m13-8 5 11" />
                  <circle class="debris" cx="105" cy="63" r="2" />
                  <circle class="debris" cx="119" cy="70" r="2.5" />
                  <circle class="debris" cx="137" cy="62" r="1.8" />
                </g>
              </svg>
            </span>
            <time datetime="2026-07">July</time>
            <span>Application lost</span>
          </div>
        </div>
      </section>

    </div>
  </div>
</figure>

<style>
  .connector-timeline {
    margin: 0 0 2.5rem;
    background: var(--bg-surface);
    border: 3px solid var(--black);
    box-shadow: 6px 6px 0 var(--coral);
    overflow: hidden;
  }

  figcaption {
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    gap: 1rem;
    padding: 0.8rem 1rem;
    background: var(--yellow);
    border-bottom: 3px solid var(--black);
    color: var(--accent-ink);
  }

  figcaption strong {
    font-family: var(--font-heading);
    font-size: 1.1rem;
    font-weight: 800;
  }

  figcaption span {
    font-family: var(--font-mono);
    font-size: 0.7rem;
  }

  .timeline-scroll {
    overflow-x: auto;
    scrollbar-width: thin;
  }

  .timeline-board {
    min-width: 760px;
    padding: 1.25rem 1rem 1rem;
  }

  .timeline-lane {
    display: grid;
    grid-template-columns: 105px 1fr;
    align-items: stretch;
  }

  .timeline-lane + .timeline-lane {
    margin-top: 0.4rem;
    padding-top: 0.9rem;
    border-top: 2px dashed var(--table-grid);
  }

  .provider {
    align-self: start;
    justify-self: start;
    margin: 0.25rem 0 0;
    padding: 0.25rem 0.65rem;
    border: 2px solid var(--black);
    border-radius: var(--radius-full);
    color: var(--accent-ink);
    font-family: var(--font-heading);
    font-size: 0.8rem;
    font-weight: 800;
    line-height: 1.2;
  }

  .anthropic-lane .provider {
    background: var(--coral);
  }

  .openai-lane .provider {
    background: var(--teal);
  }

  .track {
    min-height: 92px;
    display: grid;
    grid-template-columns: repeat(5, minmax(110px, 1fr));
    position: relative;
  }

  .rail {
    height: 4px;
    position: absolute;
    top: 19px;
    z-index: 0;
    background: var(--divider);
  }

  .anthropic-lane .rail {
    left: 10%;
    right: 10%;
    top: 50px;
    height: 3px;
    box-shadow: 0 7px 0 var(--divider);
  }

  .anthropic-lane .rail::before {
    content: "";
    position: absolute;
    inset: -4px 0 -11px;
    z-index: -1;
    background: repeating-linear-gradient(
      90deg,
      transparent 0 13px,
      var(--table-grid) 13px 17px,
      transparent 17px 29px
    );
  }

  .openai-lane .rail {
    left: 10%;
    width: 20%;
    top: 54px;
    height: 3px;
    background: var(--teal);
    box-shadow: 0 7px 0 var(--teal);
  }

  .openai-lane .rail::before {
    content: "";
    position: absolute;
    inset: -4px 0 -11px;
    z-index: -1;
    background: repeating-linear-gradient(
      90deg,
      transparent 0 13px,
      color-mix(in srgb, var(--teal) 55%, var(--bg-surface)) 13px 17px,
      transparent 17px 29px
    );
  }

  .event {
    padding: 39px 0.35rem 0;
    position: relative;
    z-index: 1;
    text-align: center;
  }

  .event::before {
    content: "";
    width: 13px;
    height: 13px;
    position: absolute;
    left: 50%;
    top: 13px;
    background: var(--bg-surface);
    border: 3px solid var(--teal);
    border-radius: 50%;
    transform: translateX(-50%);
  }

  .event time,
  .event > span:last-child {
    display: block;
  }

  .event time {
    color: var(--teal);
    font-family: var(--font-mono);
    font-size: 0.68rem;
    font-weight: 700;
    line-height: 1.2;
  }

  .event > span:last-child {
    margin-top: 0.25rem;
    color: var(--text-muted);
    font-size: 0.68rem;
    font-weight: 600;
    line-height: 1.25;
  }

  .column-1 { grid-column: 1; }
  .column-2 { grid-column: 2; }
  .column-3 { grid-column: 3; }
  .column-4 { grid-column: 4; }
  .column-5 { grid-column: 5; }
  .derail::before,
  .approved::before {
    display: none;
  }

  .anthropic-lane .track {
    min-height: 128px;
  }

  .anthropic-lane .event:not(.derail) {
    padding-top: 76px;
  }

  .anthropic-lane .event:not(.derail)::before {
    top: 44px;
  }

  .derail {
    padding-top: 85px;
  }

  .derailment {
    width: 142px;
    height: 86px;
    position: absolute;
    left: 50%;
    top: -20px;
    z-index: 3;
    transform: translateX(-48%);
  }

  .derailment svg {
    width: 100%;
    height: 100%;
    display: block;
    overflow: visible;
    stroke: var(--accent-ink);
    stroke-linecap: round;
    stroke-linejoin: round;
    stroke-width: 2.3;
  }

  .wrecked-train {
    transform: rotate(17deg);
    transform-box: fill-box;
    transform-origin: 45% 70%;
  }

  .train-cab,
  .train-nose {
    fill: var(--coral);
  }

  .train-boiler,
  .train-roof,
  .train-dome {
    fill: var(--yellow);
  }

  .train-stack,
  .train-base {
    fill: var(--teal);
  }

  .train-window {
    fill: var(--bg-surface);
  }

  .cowcatcher {
    fill: none;
  }

  .wheel {
    fill: var(--accent-ink);
  }

  .wheel-hub {
    fill: var(--yellow);
    stroke-width: 1.5;
  }

  .drive-rod {
    fill: none;
    stroke: var(--coral);
    stroke-width: 3;
  }

  .flame {
    fill: var(--coral);
    stroke: var(--accent-ink);
    transform-box: fill-box;
    transform-origin: center bottom;
    animation: flame-flicker 480ms ease-in-out infinite alternate;
  }

  .flame-top,
  .person-flame {
    fill: var(--yellow);
    animation-delay: -220ms;
  }

  .smoke-puff {
    fill: var(--text-muted);
    stroke: var(--accent-ink);
    opacity: 0.8;
    transform-box: fill-box;
    transform-origin: center;
    animation: smoke-drift 1.8s ease-in-out infinite alternate;
  }

  .smoke-puff-2 { animation-delay: -600ms; }
  .smoke-puff-3 { animation-delay: -1.1s; }

  .person-head {
    fill: var(--yellow);
  }

  .person {
    fill: none;
    stroke-width: 3;
  }

  .escapee {
    transform-box: fill-box;
    transform-origin: center;
    animation: tiny-panic 380ms ease-in-out infinite alternate;
  }

  .panic-caption {
    fill: var(--coral);
    stroke: none;
    font-family: var(--font-mono);
    font-size: 7px;
    font-weight: 900;
  }

  .falling-rail {
    fill: none;
    stroke: var(--divider);
    stroke-width: 3;
  }

  .falling-sleeper {
    fill: none;
    stroke: var(--table-grid);
    stroke-width: 4;
  }

  .debris {
    fill: var(--divider);
    stroke: none;
  }

  @keyframes flame-flicker {
    to { transform: scaleX(0.82) scaleY(1.12) rotate(4deg); }
  }

  @keyframes smoke-drift {
    to { transform: translate(-3px, -2px) scale(1.08); opacity: 0.62; }
  }

  @keyframes tiny-panic {
    to { transform: translate(1px, -1px) rotate(3deg); }
  }

  .derail time,
  .derail > span:last-child {
    color: var(--coral);
    font-weight: 800;
  }

  .openai-lane .track {
    min-height: 108px;
  }

  .openai-lane .event {
    padding-top: 80px;
  }

  .openai-lane .event:not(.approved)::before {
    top: 48px;
  }

  .happy-arrival {
    width: 96px;
    height: 68px;
    display: block;
    position: absolute;
    left: calc(50% - 22px);
    top: 0;
    z-index: 5;
    transform: translateX(-50%);
  }

  .happy-arrival svg {
    width: 100%;
    height: 100%;
    display: block;
    overflow: visible;
    stroke: var(--accent-ink);
    stroke-linecap: round;
    stroke-linejoin: round;
    stroke-width: 2;
  }

  .happy-cab,
  .happy-boiler {
    fill: var(--teal);
  }

  .happy-roof,
  .happy-base {
    fill: var(--coral);
  }

  .happy-window {
    fill: var(--bg-surface);
  }

  .happy-stack {
    fill: var(--yellow);
  }

  .happy-wheel {
    fill: var(--accent-ink);
  }

  .happy-hub {
    fill: var(--yellow);
    stroke-width: 1.25;
  }

  .happy-smoke circle {
    fill: var(--bg-surface);
    stroke: var(--teal);
    stroke-width: 1.5;
  }

  .confetti {
    fill: none;
    stroke: var(--teal);
    stroke-width: 2;
  }

  .toot-caption {
    fill: var(--teal);
    stroke: none;
    font-family: var(--font-mono);
    font-size: 7px;
    font-weight: 900;
  }

  .check {
    width: 28px;
    height: 28px;
    display: grid !important;
    place-items: center;
    position: absolute;
    left: 50%;
    top: 43px;
    z-index: 4;
    background: var(--teal);
    border: 2px solid var(--black);
    border-radius: 50%;
    color: var(--accent-ink);
    font-family: var(--font-heading);
    font-size: 1rem;
    font-weight: 900;
    line-height: 1;
    transform: translateX(-50%);
  }

  .approved time,
  .approved > span:last-child {
    color: var(--teal);
    font-weight: 800;
  }

  @media (max-width: 845px) {
    figcaption {
      align-items: flex-start;
      flex-direction: column;
      gap: 0.1rem;
    }

    .timeline-scroll {
      overflow: visible;
    }

    .timeline-board {
      min-width: 0;
      padding: 1rem 0.8rem;
    }

    .timeline-lane {
      display: block;
    }

    .timeline-lane + .timeline-lane {
      margin-top: 0.8rem;
      padding-top: 1rem;
    }

    .provider {
      margin: 0;
    }

    .track,
    .anthropic-lane .track,
    .openai-lane .track {
      min-height: 0;
      display: grid;
      grid-template-columns: minmax(0, 1fr);
      gap: 0.2rem;
      margin-top: 0.55rem;
    }

    .column-1,
    .column-2,
    .column-3,
    .column-4,
    .column-5 {
      grid-column: 1;
    }

    .rail,
    .anthropic-lane .rail,
    .openai-lane .rail {
      width: 3px;
      height: auto;
      left: 1.2rem;
      right: auto;
      top: 1.5rem;
      bottom: 1.5rem;
      box-shadow: 7px 0 0 var(--divider);
    }

    .openai-lane .rail {
      background: var(--teal);
      box-shadow: 7px 0 0 var(--teal);
    }

    .rail::before,
    .anthropic-lane .rail::before,
    .openai-lane .rail::before {
      inset: 0 -11px 0 -4px;
      background: repeating-linear-gradient(
        180deg,
        transparent 0 11px,
        var(--table-grid) 11px 15px,
        transparent 15px 25px
      );
    }

    .openai-lane .rail::before {
      background: repeating-linear-gradient(
        180deg,
        transparent 0 11px,
        color-mix(in srgb, var(--teal) 55%, var(--bg-surface)) 11px 15px,
        transparent 15px 25px
      );
    }

    .event,
    .anthropic-lane .event:not(.derail),
    .openai-lane .event {
      min-height: 2.4rem;
      display: grid;
      grid-template-columns: 3.6rem minmax(0, 1fr);
      align-items: center;
      gap: 0.45rem;
      padding: 0.55rem 0.4rem 0.55rem 3.25rem;
      text-align: left;
    }

    .event::before,
    .anthropic-lane .event:not(.derail)::before,
    .openai-lane .event:not(.approved)::before {
      left: 1.42rem;
      top: 50%;
      transform: translate(-50%, -50%);
    }

    .event > span:last-child {
      margin-top: 0;
    }

    .approved {
      min-height: 0 !important;
      padding-top: 5.25rem !important;
    }

    .happy-arrival {
      left: 3.25rem;
      top: 0;
      transform: none;
    }

    .check {
      left: 7.25rem;
      top: 2.7rem;
      transform: none;
    }

    .derail {
      min-height: 0 !important;
      padding-top: 6rem !important;
    }

    .derailment {
      width: 132px;
      height: 79px;
      left: 3.15rem;
      top: 0;
      transform: none;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .flame,
    .smoke-puff,
    .escapee {
      animation: none;
    }
  }
</style>
